<Window x:Class="ProdavnicaApp.Views.ConfirmOrderView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{DynamicResource OrderConfirmation}"
        Height="400" Width="500"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="{DynamicResource MaterialDesignPaper}"
        Foreground="{DynamicResource MaterialDesignBodyLight}">

	<Grid Margin="15">
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="*"/>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="Auto"/>
		</Grid.RowDefinitions>

		<TextBlock Grid.Row="0" Text="{DynamicResource ConfirmOrderDetails}"
                   FontSize="16" FontWeight="Bold" Margin="0,0,0,10"
                   Foreground="{DynamicResource MaterialDesignBody}" />

		<ListBox x:Name="StavkeListBox" Grid.Row="1" Margin="0,0,0,10"
                 Background="White" Foreground="Black" />

		<StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10">
			<TextBlock Text="{DynamicResource Total}" FontWeight="Bold"
                       Foreground="{DynamicResource MaterialDesignBody}" />
			<TextBlock x:Name="UkupnoTextBlock" Margin="5,0,0,0"
                       Foreground="{DynamicResource MaterialDesignBody}" />
		</StackPanel>

		<StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,20,0,0">
			<Button Content="{DynamicResource Confirm}" Width="100" Margin="10" Click="Potvrdi_Click"/>
			<Button Content="{DynamicResource Cancel}" Width="100" Margin="10" Click="Otkazi_Click"/>
		</StackPanel>
	</Grid>
</Window>
