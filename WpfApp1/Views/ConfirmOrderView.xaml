<Window x:Class="ProdavnicaApp.Views.ConfirmOrderView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{DynamicResource OrderConfirmation}" Height="250" Width="400"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="{DynamicResource MaterialDesignPaper}"
        Foreground="{DynamicResource MaterialDesignBodyLight}">

    <StackPanel Margin="20" VerticalAlignment="Center">
        <StackPanel Orientation="Horizontal" Margin="0,0,0,5">
            <TextBlock Text="{DynamicResource ProductName}" FontWeight="Bold"
                       Foreground="{DynamicResource MaterialDesignBody}"/>
            <TextBlock x:Name="ProizvodTextBlock" Margin="5,0,0,0"
                       Foreground="{DynamicResource MaterialDesignBody}"/>
        </StackPanel>

        <StackPanel Orientation="Horizontal" Margin="0,0,0,5">
            <TextBlock Text="{DynamicResource ConfirmQuantity}" FontWeight="Bold"
                       Foreground="{DynamicResource MaterialDesignBody}"/>
            <TextBlock x:Name="KolicinaTextBlock" Margin="5,0,0,0"
                       Foreground="{DynamicResource MaterialDesignBody}"/>
        </StackPanel>

        <StackPanel Orientation="Horizontal" Margin="0,0,0,5">
            <TextBlock Text="{DynamicResource ProductPrice}" FontWeight="Bold"
                       Foreground="{DynamicResource MaterialDesignBody}"/>
            <TextBlock x:Name="CijenaTextBlock" Margin="5,0,0,0"
                       Foreground="{DynamicResource MaterialDesignBody}"/>
        </StackPanel>

        <StackPanel Orientation="Horizontal" Margin="0,0,0,20">
            <TextBlock Text="{DynamicResource Total}" FontWeight="Bold"
                       Foreground="{DynamicResource MaterialDesignBody}"/>
            <TextBlock x:Name="UkupnoTextBlock" Margin="5,0,0,0"
                       Foreground="{DynamicResource MaterialDesignBody}"/>
        </StackPanel>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
            <Button Content="{DynamicResource Confirm}" Width="100" Margin="10" Click="Potvrdi_Click"/>
            <Button Content="{DynamicResource Cancel}" Width="100" Margin="10" Click="Otkazi_Click"/>
        </StackPanel>
    </StackPanel>
</Window>
