<Window x:Class="ProdavnicaApp.Views.PaymentView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{DynamicResource TitlePayment}"
        Height="300" Width="500"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="{DynamicResource MaterialDesignPaper}"
        Foreground="{DynamicResource MaterialDesignBodyLight}">

    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Margin="20">
        <TextBlock Text="{DynamicResource ChoosePayment}" 
                   FontSize="20" FontWeight="Bold" Margin="0,0,0,20" 
                   HorizontalAlignment="Center"
                   Foreground="{DynamicResource MaterialDesignBody}"/>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
            <Button Click="CardPayment_Click" Height="100">
                <Image Source="/Resources/Payments/card.png" Width="80" Height="80"/>
            </Button>
            <Button Click="CashPayment_Click" Height="100">
                <Image Source="/Resources/Payments/cash.png" Width="80" Height="80"/>
            </Button>
            <Button Click="PaypalPayment_Click" Height="100">
                <Image Source="/Resources/Payments/paypal.png" Width="80" Height="80"/>
            </Button>
        </StackPanel>
    </StackPanel>
</Window>
