<Window x:Class="ProdavnicaApp.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="{DynamicResource TitleLogin}" Height="500" Width="450"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="{DynamicResource MaterialDesignPaper}"
        Foreground="{DynamicResource MaterialDesignBodyLight}">

    <DockPanel LastChildFill="True" Margin="10">

        <StackPanel Orientation="Horizontal" DockPanel.Dock="Top" HorizontalAlignment="Right" Margin="0,0,0,10">
            
            <ComboBox Width="130" SelectionChanged="Language_SelectionChanged" Margin="0,0,10,0">
                <ComboBoxItem Tag="sr" IsSelected="True">
                    <StackPanel Orientation="Horizontal">
                        <Image Source="/Resources/Flags/sr.png" Width="20" Height="15" Margin="0,0,5,0"/>
                        <TextBlock Text="Srpski" 
                                   Foreground="{DynamicResource MaterialDesignBody}"/>
                    </StackPanel>
                </ComboBoxItem>
                <ComboBoxItem Tag="en">
                    <StackPanel Orientation="Horizontal">
                        <Image Source="/Resources/Flags/en.png" Width="20" Height="15" Margin="0,0,5,0"/>
                        <TextBlock Text="English" 
                                   Foreground="{DynamicResource MaterialDesignBody}"/>
                    </StackPanel>
                </ComboBoxItem>
            </ComboBox>

            <ComboBox x:Name="ThemeComboBox" Width="150" SelectionChanged="ThemeComboBox_SelectionChanged"
                      Foreground="{DynamicResource MaterialDesignBody}">
                <ComboBoxItem Content="{DynamicResource ThemeFirst}" Tag="LightPurpleLime" IsSelected="True"/>
                <ComboBoxItem Content="{DynamicResource ThemeSecond}" Tag="DarkBlueAmber" />
                <ComboBoxItem Content="{DynamicResource ThemeThird}" Tag="LightGreenPink" />
            </ComboBox>
        </StackPanel>

        <materialDesign:Card Padding="30" Margin="20">
            <StackPanel>
                <TextBlock Text="{DynamicResource WelcomeLogin}" 
                           FontSize="22" FontWeight="Bold" Margin="0,0,0,20"
                           HorizontalAlignment="Center"
                           Foreground="{DynamicResource MaterialDesignBody}"/>

                <TextBox x:Name="EmailTextBox"
                         Style="{StaticResource MaterialDesignOutlinedTextBox}"
                         materialDesign:HintAssist.Hint="{DynamicResource Email}"
                         Margin="0,5"
                         KeyboardNavigation.TabIndex="1"
                         ToolTip="{DynamicResource EmailTip}"
                         Foreground="{DynamicResource MaterialDesignBody}"/>

                <PasswordBox x:Name="PasswordBox"
                             Style="{StaticResource MaterialDesignOutlinedPasswordBox}"
                             materialDesign:HintAssist.Hint="{DynamicResource Password}"
                             Margin="0,15,0,5"
                             KeyboardNavigation.TabIndex="2"
                             ToolTip="{DynamicResource PasswordTip}"
                             Foreground="{DynamicResource MaterialDesignBody}"/>

                <Button Content="{DynamicResource Login}" 
                        Height="40" Margin="0,20,0,0"
                        Style="{StaticResource MaterialDesignRaisedButton}"
                        Click="Login_Click"
                        KeyboardNavigation.TabIndex="3"
                        HorizontalAlignment="Right"/>

                <TextBlock Margin="0,25,0,0" HorizontalAlignment="Center"
                           Foreground="{DynamicResource MaterialDesignBodySecondary}"
                           FontSize="12">
                    <Run Text="{DynamicResource NoAccount}" />
                    <Hyperlink Click="OpenRegister_Click">
                        <Run Text="{DynamicResource RegisterHere}" />
                    </Hyperlink>
                </TextBlock>
            </StackPanel>
        </materialDesign:Card>
    </DockPanel>
</Window>
