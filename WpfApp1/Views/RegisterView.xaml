<Window x:Class="ProdavnicaApp.Views.RegisterView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="{DynamicResource TitleRegister}"
        Height="600" Width="400"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="{DynamicResource MaterialDesignPaper}"
        Foreground="{DynamicResource MaterialDesignBodyLight}">

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="30" VerticalAlignment="Center" HorizontalAlignment="Center">

            <TextBlock Text="{DynamicResource RegisterTitle}"
                       FontSize="22" FontWeight="Bold"
                       HorizontalAlignment="Center"
                       Margin="0,0,0,20"
                       Foreground="{DynamicResource MaterialDesignBody}"/>

            <TextBox x:Name="ImeTextBox"
                     Width="300" Margin="0,5"
                     Style="{StaticResource MaterialDesignOutlinedTextBox}"
                     materialDesign:HintAssist.Hint="{DynamicResource FirstName}"
                     Foreground="{DynamicResource MaterialDesignBody}"/>

            <TextBox x:Name="PrezimeTextBox"
                     Width="300" Margin="0,5"
                     Style="{StaticResource MaterialDesignOutlinedTextBox}"
                     materialDesign:HintAssist.Hint="{DynamicResource LastName}"
                     Foreground="{DynamicResource MaterialDesignBody}"/>

            <TextBox x:Name="EmailTextBox"
                     Width="300" Margin="0,5"
                     Style="{StaticResource MaterialDesignOutlinedTextBox}"
                     materialDesign:HintAssist.Hint="{DynamicResource Email}"
                     Foreground="{DynamicResource MaterialDesignBody}"/>

            <PasswordBox x:Name="LozinkaBox"
                         Width="300" Margin="0,5"
                         Style="{StaticResource MaterialDesignOutlinedPasswordBox}"
                         materialDesign:HintAssist.Hint="{DynamicResource Password}"
                         Foreground="{DynamicResource MaterialDesignBody}"/>

            <PasswordBox x:Name="PotvrdaLozinkeBox"
                         Width="300" Margin="0,5"
                         Style="{StaticResource MaterialDesignOutlinedPasswordBox}"
                         materialDesign:HintAssist.Hint="{DynamicResource ConfirmPassword}"
                         Foreground="{DynamicResource MaterialDesignBody}"/>

            <Button Content="{DynamicResource Register}"
                    Width="300" Height="40"
                    Margin="0,20,0,10"
                    Click="Register_Click"
                    Style="{StaticResource MaterialDesignRaisedButton}" />

            <Button Content="{DynamicResource BackToLogin}"
                    Width="200" Height="30"
                    Click="BackToLogin_Click"
                    HorizontalAlignment="Center"
                    Style="{StaticResource MaterialDesignFlatButton}" />

        </StackPanel>
    </ScrollViewer>
</Window>
