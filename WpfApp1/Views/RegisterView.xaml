<Window x:Class="ProdavnicaApp.Views.RegisterView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="{DynamicResource TitleRegister}"
        Height="500" Width="400"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="{DynamicResource MaterialDesignPaper}"
        Foreground="{DynamicResource MaterialDesignBodyLight}">

	<Grid Margin="30">
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="*" />
		</Grid.RowDefinitions>

		<TextBlock Grid.Row="0" Text="{DynamicResource RegisterTitle}"
                   FontSize="22" FontWeight="Bold"
                   HorizontalAlignment="Center" Margin="0,0,0,20"
                   Foreground="{DynamicResource MaterialDesignBody}"/>

		<TextBox x:Name="ImeTextBox" Grid.Row="1"
                 Style="{StaticResource MaterialDesignOutlinedTextBox}"
                 materialDesign:HintAssist.Hint="{DynamicResource FirstName}"
                 Margin="0,5"
                 Foreground="{DynamicResource MaterialDesignBody}"/>

		<TextBox x:Name="PrezimeTextBox" Grid.Row="2"
                 Style="{StaticResource MaterialDesignOutlinedTextBox}"
                 materialDesign:HintAssist.Hint="{DynamicResource LastName}"
                 Margin="0,5"
                 Foreground="{DynamicResource MaterialDesignBody}"/>

		<TextBox x:Name="EmailTextBox" Grid.Row="3"
                 Style="{StaticResource MaterialDesignOutlinedTextBox}"
                 materialDesign:HintAssist.Hint="{DynamicResource Email}"
                 Margin="0,5"
                 Foreground="{DynamicResource MaterialDesignBody}"/>

		<PasswordBox x:Name="LozinkaBox" Grid.Row="4"
                     Style="{StaticResource MaterialDesignOutlinedPasswordBox}"
                     materialDesign:HintAssist.Hint="{DynamicResource Password}"
                     Margin="0,5"
                     Foreground="{DynamicResource MaterialDesignBody}"/>

		<Button Grid.Row="5" Content="{DynamicResource Register}"
                Click="Register_Click"
                Height="40" Margin="0,20,0,10"
                Style="{StaticResource MaterialDesignRaisedButton}" />

		<Button Grid.Row="6" Content="{DynamicResource BackToLogin}"
                Click="BackToLogin_Click"
                Height="30"
                Style="{StaticResource MaterialDesignFlatButton}"
                HorizontalAlignment="Center"/>
	</Grid>
</Window>
